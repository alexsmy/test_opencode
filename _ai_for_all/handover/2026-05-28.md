# Handover 28.05.2026 (часть 5) — Freemoney Challenge + Echo-loop fix

## Что сделано
1. **Echo-loop починена** — воркер запоминает время старта (`start_time`) и пропускает сообщения старше него. Теперь рестарт Render не вызывает повторную обработку старых писем.
2. **BLOCKED_SENDERS** — `freemoney@agentmail.to` в блок-листе escrow-воркера (чтобы ответы челленджа не создавали сделки).
3. **Новые API endpoints:**
   - `/api/escrow/create-inbox?username=xxx` — создаёт новый AgentMail inbox
   - `/api/escrow/send-from-inbox?inbox_id=&to=&subject=&body=` — отправляет с любого инбокса
   - `/api/escrow/check-inbox-id?inbox_id=xxx` — читает сообщения любого инбокса
4. **Создан agent-x01@agentmail.to** — свежий inbox для челленджа.
5. **7-я попытка freemoney** (agent-x01 → "agent-x01 wants to play", value exchange angle) → **отклонено**. Агент: "the agent got offended. start a new thread with a better angle."
6. **Выяснено:** другие проходят челлендж через музыкальную отсылку (владелец подтвердил).

## Состояние кода
- Ветка `feat/escrow-agent` на `alexsmy/bot_29` — 15+ коммитов
- Render: https://bot-29-nx0w.onrender.com — развёрнут, воркер работает
- Ключ в env `AGENTMAIL_API_KEY` на Render

## Известные проблемы
- Пустое тело ответного письма (reply_to_message) — не исправлено
- processed_ids.json не работает на Render (эфемерная ФС) — заменено на timestamp-фильтр
- CloudFront блокирует прямые запросы к AgentMail API с ПК alexs

## Следующие шаги
1. Создать agent-x02@agentmail.to
2. Отправить freemoney письмо с музыкальной отсылкой
3. Если не сработает — повторить с x03, x04 и т.д.

## Новые endpoint URL
- `https://bot-29-nx0w.onrender.com/api/escrow/create-inbox?username=xxx`
- `https://bot-29-nx0w.onrender.com/api/escrow/send-from-inbox?inbox_id=xxx&to=yyy&subject=zzz&body=www`
- `https://bot-29-nx0w.onrender.com/api/escrow/check-inbox-id?inbox_id=xxx`

## Коммиты дня
- `4ea44af` feat: add BLOCKED_SENDERS to ignore freemoney@agentmail.to
- `7b90f40` fix: persist processed_ids to disk to survive worker restarts
- `206322f` feat: add /create-inbox and /send-from-inbox endpoints
- `85f9d17` feat: add /check-inbox-id endpoint
- `dbd119a` fix: skip messages older than worker start time
