# Handover: 28.05.2026 (часть 11)

## Тема
Mail Agent v2: автоответ, FileVault, Folder Tree Bug

## Ветка bot_29
`fix/auto-reply-filevault` — 3 коммита, запушена. Render переключен.

## Что сделано

1. **Автоответ**: AgentMail API ждёт `"text"`, не `"body"`. Исправлено в `mail_client.py` + `escrow_service.py`.
2. **FileVault**: Добавлена `_save_to_filevault()` — регистрирует тело письма и вложения в FileVault.
3. **Подпапки**: `create_mail_subfolder()` — каждое письмо в своей подпапке внутри Mail.
4. **Синхронизация**: `signal_sync_needed()` вызывается после записи в FileVault.
5. **Folder tree crash**: Латентный баг в `filevault_api.py:284` — при первой же подпапке падал с `TypeError`. Исправлено: `index[child["folder_id"]]`.
6. **Тесты**: 19 зелёных, добавил `test_folder_tree_with_subfolders`.

## Статус
✅ Всё работает: Telegram, автоответ, /files, синхронизация.

## Что не сделано
- Мёртвый код не трогал
- escrow v1 не менял (кроме `body`→`text`)
- `migrateLegacyVaultKeyIfNeeded` оставлена

## Вопросы
- Нужно ли добавить `sync_on_event: true` в `config/sync_settings.json` для мгновенной синхронизации?
