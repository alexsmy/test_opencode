# Handover: 24.05.2026

## Что сделано
1. Обновлены локальные файлы bot_29 до `codex/__34` (commit `ed3b406`)
2. Создана структура `_ai_for_all/` — облачная память AI для всех проектов
3. Созданы и заполнены:
   - `README.md` — точка входа
   - `PROFILE.md` — профиль alexs
   - `AGENTS.md` — правила AI-агента
   - `CONTEXT.md` — текущий контекст
   - `SESSION_LOG.md` — история сессий (append-only)
   - `rules/` — general, git, security, telegram, sync, backup, lifecycle
   - `handover/` — детальные логи по датам
   - `PROJECTS/` — bot_29.md, TEMPLATE.md
   - `VAULT/` — crypto.js, encrypt.ps1, decrypt.ps1, variables.json.enc
   - `REFERENCE/` — urls.md, commands.md
4. Зашифрованы секреты в `VAULT/variables.json.enc` (AES-256-GCM)

## Статус
- bot_29: ветка `codex/__34`, работает на Render
- _ai_for_all: создан, требует пуша в test_opencode
- Мастер-пароль для VAULT: у alexs

## Корректировка после ревью alexs
- PROFILE: "начинающий пользователь", узлы упрощены до "любые устройства"
- README: test_opencode как облачный дом, bot_29 как текущий проект
- AGENTS: принцип "synchronization/ не удалять", облачный дом
- CONTEXT: `synchronization/` — НИКОГДА НЕ УДАЛЯТЬ
- Запушено в test_opencode/main

## Создан первый тест: vault storage CRUD

- `tests/test_vault_storage.py` — 11 шагов, проверяет все CRUD-операции vault
- Найдена мелочь: `search_entries()` — query необязателен (исправлено)
- `rules/coding.md` — добавлен раздел про тесты
- Запушено в bot_29 (ветка `test-vault-storage`)

## Добавлен rules/deepseek.md + оптимизация coding.md

- Создан `rules/deepseek.md` (13 правил: базовые принципы, режим агента, безопасность)
- `rules/coding.md` сокращён, дубликаты убраны, осталось только уникальное (объяснения, комментарии, ссылки, самообучение)
- `AGENTS.md` обновлён ссылками

## Создан rules/coding.md
- Правила работы с кодом на основе опроса alexs
- 6 пунктов: план, объяснения, комментарии, выбор решений, ссылки, обратная связь
- Будет дополняться автоматически по мере работы

## Файлы на удаление (старая версия)
- `_ai/` в bot_29 — остаётся как есть, можно удалить после пуша _ai_for_all

---

## 24.05.2026 (2-я часть) — Тест 2FA + рефакторинг sync_service

### Создан тест device_auth
- `tests/test_device_auth.py` — 10 шагов
  - Шаг 1: устройство не авторизовано изначально
  - Шаг 2: запрос кода → генерация + сохранение
  - Шаг 3: неверный код → отклонён, счётчик растёт
  - Шаг 4: верный код → авторизация + сессия
  - Шаг 5: устройство значится авторизованным
  - Шаг 6: повторный запрос — `already_authorized: True`
  - Шаг 7: просроченный код → отклонён
  - Шаг 8: 6 неверных → блокировка
  - Шаг 9: анти-флуд (частая отправка)
  - Шаг 10: удаление устройства → деавторизация
- Итог: устройство 2FA работает корректно, багов не найдено

### Рефакторинг sync_service (1255л → 9 модулей)
- `services/sync_service.py` разбит на пакет `services/sync/`:
  - `utils.py` — `_compute_hash`, `_now_iso`, `_is_meaningful_json`
  - `settings.py` — настройки синхронизации
  - `manifest.py` — манифест отправленных файлов
  - `chunking.py` — дробление больших файлов, сборка частей
  - `github.py` — все запросы к GitHub API
  - `collectors.py` — сбор данных (keepalive, filevault, crpt, agents, vault)
  - `restore.py` — восстановление с GitHub
  - `notify.py` — Telegram-уведомления
  - `worker.py` — оркестрация sync, триггеры, фоновый воркер
- `sync_service.py` — shim (2 строки) для обратной совместимости
- Все 11 внешних импортов продолжают работать

### Создан тест чистых функций sync
- `tests/test_sync_service_pure.py` — 17 шагов
  - `_compute_hash`: строка, bytes, пустой ввод, детерминированность
  - `_chunk_if_large`: маленький файл, большой (3 части), ровно по границе, остаток
  - `_is_meaningful_json`: не существует, пустой, пробелы, невалидный, `[]`, `{}`, непустые, число

### Git
- Ветка `refactor/sync-package` запушена в bot_29
- Ветка `test-device-auth` — локально (не запушена)
- Изменения в `_ai_for_all` будут запушены в test_opencode/main
