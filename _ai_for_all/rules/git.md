# Git Rules

- **Никогда не пушить в ту же ветку** — создавать новую с понятным названием
- Не коммитить без разрешения alexs
- Commit message: кратко на русском
- Не использовать `--force`, не переписывать историю
- Не хранить токены/пароли/ключи в коде
- `secrets/` — не трекать в git

## Исключения
- `_ai_for_all/` в `test_opencode`: пуш в `main` ТОЛЬКО с подтверждения alexs
